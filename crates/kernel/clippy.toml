disallowed-types = [
  { path = "alloc::boxed::Box", reason = "kernel code must not allocate in the hot path; use bounded caller-owned storage" },
  { path = "alloc::collections::BTreeMap", reason = "kernel hot-path collections must be bounded and caller-owned" },
  { path = "alloc::collections::BTreeSet", reason = "kernel hot-path collections must be bounded and caller-owned" },
  { path = "alloc::collections::BinaryHeap", reason = "kernel hot-path collections must be bounded and caller-owned" },
  { path = "alloc::collections::LinkedList", reason = "kernel hot-path collections must be bounded and caller-owned" },
  { path = "alloc::collections::VecDeque", reason = "kernel hot-path collections must be bounded and caller-owned" },
  { path = "alloc::rc::Rc", reason = "kernel state should have explicit ownership and no heap reference counting" },
  { path = "alloc::string::String", reason = "kernel protocol data should use fixed-size or bounded byte storage" },
  { path = "alloc::sync::Arc", reason = "kernel state should have explicit ownership and no heap reference counting" },
  { path = "alloc::vec::Vec", reason = "kernel hot-path collections must be bounded and caller-owned" },
  { path = "std::boxed::Box", reason = "kernel code must not allocate in the hot path; use bounded caller-owned storage" },
  { path = "std::collections::BTreeMap", reason = "kernel hot-path collections must be bounded and caller-owned" },
  { path = "std::collections::BTreeSet", reason = "kernel hot-path collections must be bounded and caller-owned" },
  { path = "std::collections::BinaryHeap", reason = "kernel hot-path collections must be bounded and caller-owned" },
  { path = "std::collections::HashMap", reason = "kernel hot-path collections must be bounded and deterministic" },
  { path = "std::collections::HashSet", reason = "kernel hot-path collections must be bounded and deterministic" },
  { path = "std::collections::LinkedList", reason = "kernel hot-path collections must be bounded and caller-owned" },
  { path = "std::collections::VecDeque", reason = "kernel hot-path collections must be bounded and caller-owned" },
  { path = "std::rc::Rc", reason = "kernel state should have explicit ownership and no heap reference counting" },
  { path = "std::string::String", reason = "kernel protocol data should use fixed-size or bounded byte storage" },
  { path = "std::sync::Arc", reason = "kernel state should have explicit ownership and no heap reference counting" },
  { path = "std::sync::Mutex", reason = "kernel transitions must be deterministic and not internally synchronized" },
  { path = "std::sync::RwLock", reason = "kernel transitions must be deterministic and not internally synchronized" },
  { path = "std::time::Duration", reason = "kernel time must be explicit block context, not local wall-clock time" },
  { path = "std::time::Instant", reason = "kernel time must be explicit block context, not local wall-clock time" },
  { path = "std::time::SystemTime", reason = "kernel time must be explicit block context, not local wall-clock time" },
  { path = "std::vec::Vec", reason = "kernel hot-path collections must be bounded and caller-owned" },
]

disallowed-methods = [
  { path = "std::thread::spawn", reason = "kernel transitions must not spawn threads" },
  { path = "std::time::Instant::now", reason = "kernel time must be explicit block context, not local wall-clock time" },
  { path = "std::time::SystemTime::now", reason = "kernel time must be explicit block context, not local wall-clock time" },
]
