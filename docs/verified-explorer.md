# Verified explorer boundary

Enable `hellas-chain/verified-explorer` for the portable verifier and `wasm-client`
for a browser/Worker WebSocket transport. The canonical block decoder, certificate
checks, query binding, proof bundle, and `VerifiedStore` contract are shared code.
The existing native follower keeps its Commonware archive; the Worker supplies a
transactional SQLite implementation of `VerifiedStore`.

Provision a `hellas_genesis::TrustDocument` through authenticated deployment
configuration. Never bootstrap it from the same indexer whose answers it verifies.
The document has `schema_version: 1`, `network_id: "hellas-devnet-1"`, the lowercase
`genesis_sha256` of the exact authenticated genesis JSON bytes, and a nonempty `epochs` array. Each
epoch supplies `epoch`, `start_height`, exclusive `end_height` (or null), and a
96-character lowercase compressed BLS MinPk `threshold_identity`. Start at height
zero, leave no gaps or overlaps, and increase epoch IDs strictly. A finite final
interval rejects future heights until the trusted configuration is updated.

`trust_sha256` is SHA-256 of `serde_json::to_vec(&TrustDocument)`, not the original
JSON text. Its canonical form has no whitespace; document fields appear in order
`schema_version,network_id,genesis_sha256,epochs`; epoch fields appear in order
`epoch,start_height,end_height,threshold_identity`; `end_height` is always present
with JSON null for an open interval. All validated strings are ASCII and integer
fields use unsigned decimal without padding. Distribute the exact trusted document
with independently installed verifiers. A copy embedded in a web page is evidence
metadata, not an independent trust anchor.

`ProofBundle` has the same fields in serde JSON and protobuf (the field numbers
are specified by the `prost::Message` definition). JSON byte vectors are arrays of
integers. `ExplorerVerifier::verify` checks the pinned trust identifier, devnet,
epoch schedule, threshold certificate, block/certificate round, canonical block
bytes, payload, height, state root, and requested block or transaction identifier.
Transaction identifiers hash the canonical chain transaction encoding using
`transaction_digest`. `VerifiedBlock` exposes only verified block data for rendering.
`observed_at_ms` is advisory and is intentionally not certified; valid finality
does not establish that an answer is the newest available.

Backends must atomically store immutable canonical evidence and a monotonic head
cursor, rejecting conflicts at the same height or payload. Historical cache fills
may retain older blocks without moving the cursor backward. Cache eviction must
preserve the head cursor. Re-verify stored evidence with the current pinned trust
configuration before rendering. `VerifiedStore` futures do not require Send, so a
Cloudflare Durable Object can implement the same boundary as a native process.

Address summaries and holdings pages use `AddressProofBundle`. Its `block` is the same
certificate-backed block bundle; its bounded `page` bytes encode `OwnerPageProof` as
JSON. `verify_address` verifies both the certificate and owner proof before exposing
`VerifiedAddress`. Summary `balance` is the u128 sum of spendable coins; `count` includes
coins and edges, with an edge counted once for each distinct party. Edges contribute
zero spendable balance. No transaction-history completeness claim is made.

Consensus updates sparse owner/holdings tree nodes in the same speculative QMDB batch
as every coin/edge mutation, including genesis allocations, legacy transfers/merges,
and kernel opens/closes. Canonical `HLS2` blocks commit both the QMDB state root and the
owner root; proposer verification and certified replay recompute both. Owner metadata
uses domain-separated keys and a fixed-size internal object variant. Stored QMDB
partitions advance to v3. **This requires resetting devnet and native follower storage.**
Old canonical block encodings are rejected; no mixed-format replay is supported.

An owner path proves membership or absence. Holdings paths authenticate their ranks
with subtree counts, so a bounded page cannot omit, reorder, duplicate, or substitute
a holding. Pages accept offsets up to the authenticated count and limits 1..64.
Empty siblings are implied; supplied sibling depths must be strictly increasing and
unique. Every balance/count is authenticated as part of the tree hash. The native
origin reconstructs its owner tree from retained verified history, checks the root
against each block, and atomically publishes a snapshot only after matching it.

No live threshold key, trust reset, or deployment is generated by this library; an
operator must supply the authenticated threshold schedule.

## Native private origin

For an independently provisioned disposable devnet, export epoch-zero public trust
from the random configuration produced by `chain validator generate-network`:

```sh
hellas-cli chain validator export-trust --config network/validator-0.toml --genesis network/genesis.json > network/trust.json
hellas-cli chain indexer serve --rpc ws://127.0.0.1:8789 --genesis network/genesis.json --trust network/trust.json --storage-dir network/indexer
```

The exporter checks that the genesis matches the validator configuration and decodes
the threshold polynomial/share using the consensus codec. It outputs only public
trust data. Authenticate both documents independently of the RPC peer. The genesis
hash includes whitespace and trailing newlines: provision the **exact file contents**
as the Worker's `RELAY_GENESIS_JSON`, alongside `EXPLORER_TRUST_JSON`. Do not parse
and reserialize the genesis or trim its newline during provisioning. Only
`hellas-devnet-1` is supported. Use fresh native storage and Worker storage when
resetting the network.

`ExplorerVerifier::with_genesis(trust, genesis_bytes)` pins both authenticated
inputs and rejects a digest or network mismatch. `ExplorerVerifier::new(trust)`
and `chain indexer serve` without `--genesis` retain the embedded devnet default.

`hellas-cli chain indexer serve --rpc wss://relay.example/ws --trust trust.json
--storage-dir /var/lib/hellas/explorer --listen 127.0.0.1:8788` starts a native
verified follower and a loopback-only HTTP origin. Point a Cloudflare Tunnel at
that listener and protect the external hostname with an Access service-token
policy. The process never downloads its trust configuration from the RPC peer.

`/api/v1/blocks/{latest|height|payload}/proof` and
`/api/v1/transactions/{digest}/proof` return the shared ProofBundle, defaulting to
protobuf on `/proof` routes and selecting JSON for `Accept: application/json` (both
`application/x-protobuf` and `application/protobuf` are accepted). Other API aliases
default to JSON. Unsupported representation requests receive 406. Native canonical
block history remains durable in Commonware. Transaction locators rebuild from
verified archive records on restart; unavailable or not-yet-indexed locators return
503. A known block height can be supplied as `?height=N` to resolve a transaction
without waiting for the locator. Every response is reverified before serving.

The native origin uses a scheduled Commonware certificate provider and height epocher
from the authenticated trust document. It rejects wrong-height keys and unknown epochs
before archive ingestion. Native epoch IDs must be consecutive beginning at zero;
finite schedules fail closed past their final height. Changing a schedule requires
restarting the follower with newly authenticated configuration. Core validators still
need their own coordinated signing-key/epoch activation before a rotated schedule
produces blocks. Address proofs default to the latest rebuilt verified snapshot; the origin retains
the most recent 32 snapshots by finalized height for payload-pinned pagination.

`/api/v1/addresses/{base58-owner}/proof?offset=0&limit=64` returns an address bundle;
`payload=...` pins a requested snapshot across subsequent blocks and returns 503
when that snapshot falls outside the native origin's 32-snapshot retention window. During replay/catch-up no unverified address snapshot is served.
The edge may retain older independently verifiable bundles in its bounded cache.

Native snapshot retention is indexed by both payload and height, rejects conflicting
bindings, and evicts the lowest finalized height first. An in-flight request holds
its snapshot independently, so eviction cannot change the page being generated.
Snapshots are reconstructed and checked against their certified owner roots before
entering retention; restarting the process rebuilds the same bounded window from
its durable finalized archive.
