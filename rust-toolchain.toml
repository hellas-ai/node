[toolchain]
# Keep local and hosted CI diagnostics reproducible. Upgrade this deliberately
# with the compatibility fixes that a newer compiler requires.
channel = "1.96.1"
components = [
    "rustfmt",
    "clippy",
    "rust-src",
    "rust-analyzer",
    "llvm-tools-preview",
]
